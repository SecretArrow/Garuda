<?xml version="1.0" encoding="utf-8"?>
<!--
  Motion Browser network security.
  Default posture: HTTPS-only (mirrors android:usesCleartextTraffic="false").
  Cleartext is permitted ONLY for loopback / emulator-host addresses so the
  on-device AI mock server (instrumented tests) and local dev proxies work.
  Every real-world site stays on TLS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
